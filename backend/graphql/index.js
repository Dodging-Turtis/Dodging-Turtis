const mongoose = require('mongoose');
import './db';
const auth = require('./models/auth');
const turtle = require('./models/turtle');

console.log('Dodging Turtis v2.0');

export default async function handler(req, res) {
  res.status(404);
}
